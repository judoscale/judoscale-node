{
  "name": "judoscale-node",
  "private": true,
  "workspaces": [
    "packages/*",
    "sample_apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^8.57.1",
    "eslint": "^8.57.1",
    "globals": "^17.6.0",
    "prettier": "^3.8.4"
  }
}
