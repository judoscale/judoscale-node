#!/bin/bash

# Exit immediately if a command exits with a non-zero status.
set -e

# Print each command before executing it:
set -x

cd node-core
npm run test

cd ../express
npm run test

cd ../fastify
npm run test

cd ../bullmq
npm run test
